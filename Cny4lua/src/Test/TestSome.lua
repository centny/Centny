--this file is testing the normal thing.

--the normal block.
do --block begin
	local x=100	--define the local value.
	print(x)
end --block end.

--test repeat
repeat
	print("runnging when the condition is always true") --this statements will run al last one time.
until true
